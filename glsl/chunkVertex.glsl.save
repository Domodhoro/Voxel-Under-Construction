#version 300 es

layout(location = 0) in vec4 position;
layout(location = 21) in vec3 texture;

uniform mat4 model, view, projection;

out highp vec3 Color, Texture;

void main() {
    gl_Position = projection * view * model * vec4(position.xyz, 1.0f);

    Color = color;
    Texture = texture;
}
